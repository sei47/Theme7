<h3>以下の内容で、送信する</h3>

<%= form_with(model: @picture ,url: pictures_path, local: true) do |form| %>
  <%= form.hidden_field :image %>
  <%= form.submit "登録する" %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>

<%= link_to "戻る", new_picture_path %>
